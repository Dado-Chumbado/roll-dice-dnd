version: '3.3'

services:
  bots:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /var/lib/roll-dice-dnd:/data
    restart: unless-stopped
    env_file:
      - ./src/.env
    command: ["/app/start-bots.sh"]